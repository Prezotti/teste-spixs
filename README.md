# teste-spixs

Copie `.env.example` para `.env` e preencha `GOOGLE_MAPS_API_KEY`.
O `.env` não entra no git.

No Google Cloud, ative Places API (New), Routes API, Maps SDK for Android e Maps SDK for iOS.

